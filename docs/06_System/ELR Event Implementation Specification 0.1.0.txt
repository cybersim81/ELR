================================================================================
English Learning Repository (ELR)

ELR Event Implementation Specification

Version: 0.1.0
Status: DRAFT
=============

# Document Identity

Document Name:

ELR Event Implementation Specification

Document Version:

0.1.0

Status:

DRAFT

Dependencies:

* ELR Event Model Specification
* ELR Application Service Implementation Specification
* ELR Core Domain Implementation Specification
* ELR Persistence Implementation Specification
* ELR Implementation Architecture Specification

================================================================================
Scopo
=====

La ELR Event Implementation Specification definisce la prima implementazione
del modello eventi dell'English Learning Repository.

Gli eventi rappresentano fatti avvenuti nel sistema.

Gli eventi:

* non comandano il sistema;
* non sostituiscono le API;
* non contengono logica applicativa.

================================================================================
Event Principles
================

EVENT-IMPLEMENTATION-001 — Facts Only

Gli eventi descrivono eventi già completati.

EVENT-IMPLEMENTATION-002 — Immutable Events

Un evento pubblicato non viene modificato.

EVENT-IMPLEMENTATION-003 — Domain Origin

Gli eventi significativi originano dal dominio o dall'application layer.

EVENT-IMPLEMENTATION-004 — Loose Coupling

I consumer non devono conoscere il produttore interno dell'evento.

================================================================================
Event Architecture
==================

Application Service

```
    |

    v
```

Domain Event Publisher

```
    |

    v
```

Event Store / Event Bus

```
    |

    +----------------+

    |                |
```

Audit Consumer   Future Services

================================================================================
Event Envelope
==============

Ogni evento utilizza una struttura comune coerente con l'ELR Event Model
Specification:

{

"event_id": "uuid",

"event_type": "string",

"event_source": "string",

"entity_id": "uuid",

"entity_type": "string",

"timestamp": "datetime",

"version": "integer",

"payload": {}

}

event_id identifica univocamente l'evento.

event_type identifica il tipo di fatto rappresentato.

event_source identifica il componente che ha prodotto l'evento.

entity_id identifica l'aggregate o l'entità a cui il fatto si riferisce.

entity_type identifica il tipo dell'aggregate o dell'entità.

timestamp identifica l'istante di registrazione dell'evento.

version identifica la versione dello schema dell'evento.

payload contiene esclusivamente le informazioni necessarie a rappresentare
il fatto avvenuto.

================================================================================
Event Envelope / Persistence Mapping
====================================

Il Transactional Event Record viene persistito nella tabella:

event_records

Mapping:

Event Envelope
    |
    v
Transactional Event Record
    |
    v
event_records

Campi:

event_id
    -> id

event_type
    -> event_type

event_source
    -> event_source

aggregate_type
    -> aggregate_type

aggregate_id
    -> aggregate_id

version
    -> version

payload
    -> payload

occurred_at
    -> occurred_at

published_at
    -> published_at

created_at
    -> created_at

metadata
    -> metadata

`event_source` e `version` devono essere persistiti nelle rispettive
colonne dedicate.

Il campo `metadata` contiene esclusivamente metadati aggiuntivi
dell'Event Envelope e non deve essere utilizzato come sostituto dei
campi strutturali `event_source` o `version`.

Il Transactional Event Record è un artefatto infrastrutturale separato
dalla persistenza delle entità del Repository di conoscenza.

La sua persistenza non introduce eventi operativi nel Persistence Model
del Repository.

Il record deve essere creato nella stessa transazione che persiste la
modifica di dominio che ha prodotto l'evento.

La pubblicazione verso consumer esterni avviene successivamente.


================================================================================
Core Events v0.1
================

================================================================================
LearningObjectCreated
=====================

Descrizione:

Un nuovo Learning Object è stato creato con lifecycle status Candidate.

Producer:

LearningObjectService

Payload:

{

```
"learning_object_id": "uuid",

"anchor_id": "uuid",

"lifecycle_status": "Candidate"
```

}

================================================================================
LearningObjectSubmitted
=======================

Descrizione:

Un Learning Object è stato inviato a revisione.

Producer:

LearningObjectService

Payload:

{

```
"learning_object_id": "uuid",

"previous_lifecycle_status": "Candidate",

"new_lifecycle_status": "Proposed"
```

}

================================================================================
ChangeProposalApproved
======================

Descrizione:

Una Change Proposal è stata approvata dalla Learning Review secondo
le regole del Review e del Lifecycle Model.

Producer:

LearningReview / Application Service

Payload:

{

"change_proposal_id": "uuid",

"learning_object_id": "uuid",

"review_id": "uuid"

}

L'evento rappresenta un fatto già avvenuto.

Non costituisce un comando e non determina autonomamente una transizione
del Learning Object.

================================================================================
ChangeProposalRejected
======================

Descrizione:

Una Change Proposal è stata rifiutata dalla Learning Review.

Producer:

LearningReview / Application Service

Payload:

{

"change_proposal_id": "uuid",

"learning_object_id": "uuid",

"review_id": "uuid",

"reason": "string"

}

L'evento rappresenta un fatto già avvenuto.

Non costituisce un comando e non determina autonomamente una transizione
del Learning Object.

================================================================================
LearningObjectUpdated
=====================

Descrizione:

Una nuova versione è stata creata.

Payload:

{

```
"learning_object_id": "uuid",

"new_version": 2
```

}

================================================================================
Event Persistence
=================

La prima implementazione utilizza un Transactional Event Record persistence
coerente con la Persistence Implementation Specification.

Il Transactional Event Record è un artefatto infrastrutturale destinato alla
successiva pubblicazione degli eventi.

Non costituisce parte del Repository di conoscenza e non introduce eventi
operativi nel Persistence Model del Repository.

PostgreSQL Transactional Event Record Table

Tabella:

event_records

Campi:

id

UUID primary key

event_type

VARCHAR

event_source

VARCHAR

aggregate_type

VARCHAR

aggregate_id

UUID

version

VARCHAR

payload

JSONB

occurred_at

TIMESTAMP

published_at

TIMESTAMP nullable

created_at

TIMESTAMP

metadata

JSONB nullable

Vincoli:

* event_type obbligatorio;
* event_source obbligatorio;
* aggregate_id obbligatorio;
* version obbligatoria;
* payload obbligatorio;
* event_source deve essere preservato per la successiva pubblicazione;
* version deve essere preservata per la successiva pubblicazione e
  interpretazione;
* un Transactional Event Record è immutabile dopo la creazione;
* published_at può essere valorizzato solo per indicare l'avvenuta
  pubblicazione.

Il Transactional Event Record viene creato nella stessa transazione che
persiste la modifica di dominio che ha prodotto l'evento.

La sua persistenza è separata dalla persistenza delle entità del Repository.

La pubblicazione verso consumer esterni avviene successivamente.

Il Transactional Event Record non modifica il perimetro del Persistence Model:
gli eventi operativi non diventano entità del Repository e non fanno parte
del modello persistente della conoscenza.


================================================================================
Event Flow Example
==================

Approve Change Proposal:


Command
    |
    v
Application Service
    |
    v
Learning Review Decision
    |
    v
Domain Transition
    |
    v
Resolve Versioning Action
    |
    v
Create New Version when required
    |
    v
Persist Entity
    |
    v
Create Transactional Event Record
    |
    v
Commit Transaction
    |
    v
Publish Event

ChangeProposalApproved

Quando la decisione approvata produce una nuova versione del Learning Object,
l'evoluzione viene inoltre rappresentata dal relativo evento
LearningObjectUpdated.

================================================================================
Event Consumers
===============

Consumer iniziali:

Audit Consumer

Responsabilità:

registrare eventi significativi.

Future:

Mastery Engine

Learning Analytics

KEP Pipeline

================================================================================
Error Handling
==============

La mancata pubblicazione evento non deve compromettere la consistenza del
dominio.

Prima implementazione:

Transactional Event Record

L'evento viene registrato nella stessa transazione
dell'operazione di dominio/applicativa.

La pubblicazione verso consumer esterni avviene successivamente
senza rendere la disponibilità del consumer una precondizione
per la consistenza del dominio.

Successivamente:

Outbox Pattern

================================================================================
Excluded Scope
==============

Non incluso:

* Kafka/RabbitMQ;
* distribuzione multi servizio;
* event streaming;
* analytics pipeline.

================================================================================
Prossimo Incremento
===================

Il prossimo documento previsto:

ELR Security Implementation Specification

che definirà il primo modello concreto di accesso e protezione.

================================================================================
Principio conclusivo
====================

Gli eventi ELR rappresentano la memoria dinamica dell'evoluzione della
conoscenza.

Il dominio rimane la fonte della verità.

================================================================================
Fine documento

English Learning Repository (ELR)

ELR Event Implementation Specification

Version: 0.1.0

================================================================================
