# 05_Implementation_Alignment_Roadmap.txt

## Implementation Alignment Roadmap

### Purpose

This roadmap converts the Implementation Baseline Inventory and End-to-End Gap Map into an ordered implementation sequence.

It is subordinate to the normative architecture.

### Guiding Rule

Resolve semantic deviations before materializing production persistence.

Do not encode a currently non-conformant domain representation into database schemas and repositories and then attempt to correct it later.

---

## Phase IA-01 — Domain Semantic Alignment

### Objective

Bring the domain representation into explicit agreement with the consolidated normative baseline.

### Scope

Resolve:

* IA-001 — LearningObject statement representation;
* IA-002 — KnowledgeStatement identity/value semantics;
* IA-003 — generic Version entity;
* IA-005 — LearningObject version semantics;
* IA-010 — generic VersionRepository history;
* IA-017 — Version snapshot representation;
* IA-018 — application/repository Version boundary;
* IA-019 — Version snapshot test coverage.

### Completed Result

The domain and repository contracts now express LearningObject-centered knowledge versioning.

KnowledgeStatement is represented as an immutable aggregate member/value and has no autonomous persistence or versioning boundary.

Version history is explicitly scoped to LearningObject.

Version snapshots preserve the structured KnowledgeStatement value.

Application and repository Version history contracts are aligned.

### Gate

`IA-01 DOMAIN ALIGNMENT`

Status: `COMPLETE`

### Closure Evidence

* Domain entities aligned;
* VersionRepository contract aligned;
* Application service aligned;
* in-memory Version repository aligned;
* Version snapshot semantics tested;
* no autonomous KnowledgeStatement repository introduced.

### Remaining Work Outside IA-01

Production persistence and repository integration remain pending and belong to subsequent implementation-alignment phases.

---

## Phase IA-02 — Contract and Lifecycle Alignment

### Objective

Resolve remaining repository and lifecycle ambiguities before persistence.

### Scope

Primarily:

* IA-009 — LearningObject delete capability;
* repository ownership boundaries;
* Version history ownership;
* Audit history boundary;
* lifecycle contract consistency.

### Verified Resolution Progress

The following IA-02 alignment findings have been resolved and verified:

* IA-009 — LearningObject delete capability — `RESOLVED`;
* IA-016 — Lifecycle Application Orchestration — `RESOLVED`.

The LearningObject Repository no longer exposes physical deletion as a Domain Repository capability.

The LearningObject lifecycle orchestration and invalid-transition coverage have been verified against:

`Candidate → Proposed → Active → Retired`

### Required Result

Every repository capability must have an explicit normative purpose.

The LearningObject lifecycle must remain:

`Candidate → Proposed → Active → Retired`

Change Proposal review decisions must remain outside the LearningObject lifecycle state.

Version history must remain LearningObject-centered.

Audit history must remain distinct from immutable Version history.

### Gate

`IA-02 CONTRACT AND LIFECYCLE ALIGNMENT`

Status: `COMPLETE`

Progress: `100%`

### Closure Evidence

* repository capability boundaries verified;
* LearningObject Repository ownership verified;
* Version history ownership verified as LearningObject-centered;
* Audit history boundary verified as distinct from Version history;
* LearningObject delete capability verified as absent from the Domain Repository contract;
* lifecycle orchestration verified against `Candidate → Proposed → Active → Retired`;
* invalid lifecycle transition behavior verified;
* Change Proposal review decisions remain outside LearningObject lifecycle state.

---

## Phase IA-03 — Persistence Model

### Objective

Materialize the aligned domain model in production persistence.

### Scope

Implement and verify persistence for:

* LearningObject;
* Version;
* AuditRecord;
* required KnowledgeStatement representation;
* Anchor relationships.

### Required Result

Persistence models represent the aligned domain semantics rather than the pre-alignment implementation.

KnowledgeStatement must remain an aggregate member/value representation and must not acquire an autonomous persistence or repository boundary merely as a consequence of persistence implementation.

Version persistence must preserve the LearningObject-centered immutable Version boundary.

### Gate

`IA-03 PERSISTENCE MODEL ALIGNMENT`

Status: `COMPLETE`

Progress: `100%`

### Closure Evidence

* `LearningObjectModel` persistence model verified;
* embedded `KnowledgeStatement` representation verified;
* Anchor relationship verified;
* `VersionModel` and LearningObject foreign key verified;
* `AuditRecordModel` verified, including the SQLAlchemy-safe `metadata` column mapping;
* LearningObject `examples` / `notes` relationships verified;
* owned relationship cascade and `delete-orphan` behavior verified;
* focused persistence-model tests passed.

### Scope Boundary

Concrete repositories belong to IA-04.

Database/session wiring, migrations, transaction handling, and persistent integration belong to IA-05 and later phases.

These concerns must not be counted as IA-03 work.

---

## Phase IA-04 — Concrete Repository Layer

### Objective

Connect domain repositories to production persistence.

### Scope

Implement concrete repositories for:

* LearningObject;
* Version;
* Audit;
* Anchor where required by the final persistence architecture.

### Required Result

Application services operate against domain repository contracts while production execution uses concrete persistence implementations.

Repository capabilities must remain consistent with the lifecycle and aggregate boundaries established by IA-02.

### Gate

`IA-04 REPOSITORY INTEGRATION`

Status: `COMPLETE`

Progress: `100%`

---

## Phase IA-05 — Database Wiring

### Objective

Make the production persistence infrastructure executable and verify its database/session/transaction boundary.

### Scope

Establish and verify:

* database configuration;
* SQLAlchemy engine/session creation;
* model metadata;
* repository lifecycle;
* transaction boundary;
* production persistence wiring.

Schema/migration concerns remain separate and must only be marked complete when independently verified.

### Verified Result

The production persistence infrastructure is executable.

The concrete service wiring connects application services to:

`LearningObject Repository → Version Repository → Audit Repository`

using the SQLAlchemy persistence layer.

A fresh SQLite runtime can create the required schema and persist LearningObject, Version and AuditRecord state.

Transaction behavior has been verified through rollback-focused integration tests.

### Gate

`IA-05 DATABASE INTEGRATION`

Status: `COMPLETE`

Progress: `100%`

### Closure Evidence

* database engine creation verified;
* session factory verified;
* SQLAlchemy model metadata verified;
* production repository wiring verified;
* transaction boundary verified;
* persistence rollback verified;
* production persistence flow verified by E2E-07;
* full test suite: `70 passed`;
* checkpoint: `b00d5c7`.

### Scope Boundary

Persistent reload and recovery across a newly created session belong to E2E-08 and subsequent E2E phases.

Schema/migration hardening remains pending unless separately verified.

---

## Phase IA-06 — Persistent Integration Tests

### Objective

Prove production persistence behavior rather than relying only on repository mocks or in-memory fixtures.

### Verified Scope

The following persistent behaviors are covered:

* LearningObject persistence;
* Version persistence;
* Audit persistence;
* Anchor relationships;
* transaction rollback;
* invalid lifecycle transition rollback;
* atomic approve() persistence;
* atomic update_knowledge() persistence;
* multi-Version persistence and ordering.

The production persistence flow is verified by dedicated integration coverage.

### Remaining Scope

The following cross-session behaviors remain:

* persistent reload through a newly created session;
* Version history recovery after session reload;
* Audit history recovery after session reload;
* complete persistent lifecycle across a session boundary.

These remaining behaviors are tracked by E2E-08 through E2E-11.

### Gate

`IA-06 PERSISTENCE TEST CONFORMANCE`

Status: `IN PROGRESS`

Progress: `PARTIAL`

Production persistence behavior is verified, but cross-session recovery is not yet complete.

### Current Next Gap

`E2E-08 — Persistent Reload`

---

## Phase IA-07 — E2E-GATE-01

### Objective

Demonstrate the complete persistent LearningObject lifecycle.

### Required Flow

Anchor
  ↓
LearningObject
  ↓
KnowledgeStatement
  ↓
Candidate
  ↓
Proposed
  ↓
Active
  ↓
Retired
  ↓
Immutable Version
  ↓
Audit Record
  ↓
Database
  ↓
Reload
  ↓
Version History
  ↓
Audit History

Change Proposal review decisions such as Reviewed, Approved, and Rejected are not LearningObject lifecycle states and must not appear in this lifecycle flow.

Gate Condition

The complete flow must succeed without semantic loss or violation of the established lifecycle, aggregate, Version, and Audit boundaries.

IA-01 Preconditions
IA-001 — RESOLVED
IA-002 — RESOLVED
IA-003 — RESOLVED
IA-005 — RESOLVED
IA-010 — RESOLVED
IA-017 — RESOLVED
IA-018 — RESOLVED
IA-019 — RESOLVED

Status

PENDING

### Prerequisite Sequence

The gate is downstream of:

`E2E-07 Production Persistence — COMPLETE`
→ `E2E-08 Persistent Reload — NEXT`
→ `E2E-09 Version History Recovery — PENDING`
→ `E2E-10 Audit History Recovery — PENDING`
→ `E2E-11 Persistent End-to-End Test — PENDING`

IA-07 MUST NOT be marked complete until the full persistent lifecycle has been verified across the session boundary.

---

## Phase IA-08 — Implementation Conformance

### Objective

Compare the resulting implementation against the authoritative normative corpus.

### Required Result

All confirmed deviations must be resolved or explicitly documented as authorized exceptions.

All critical persistence and E2E gaps must be closed.

The final implementation must preserve:

* LearningObject as the primary knowledge aggregate;
* KnowledgeStatement as an aggregate member/value;
* LearningObject-centered immutable Version history;
* the normative LearningObject lifecycle;
* separation between Change Proposal review decisions and LearningObject lifecycle;
* separation between Version history and Audit history;
* independent Anchor identity and persistence boundary.

### Gate

`IMPLEMENTATION CONFORMANCE`

Status: `PENDING`

---

# Current Roadmap Position

M0.3.2 Specification Propagation
│
▼
Implementation Baseline Inventory
│
▼
E2E Gap Map
│
▼
IA-01 Domain Semantic Alignment
│
│ COMPLETE — 100%
▼
IA-02 Contract and Lifecycle Alignment
│
│ COMPLETE — 100%
▼
IA-03 Persistence Model
│
│ COMPLETE — 100%
▼
IA-04 Concrete Repository Layer
│
│ COMPLETE — 100%
▼
IA-05 Database Wiring
│
│ COMPLETE — 100%
▼
E2E-07 Production Persistence
│
│ COMPLETE — VERIFIED
│
│ checkpoint: b00d5c7
│
│ test suite: 70 passed
▼
E2E-08 Persistent Reload
│
│ NEXT
▼
E2E-09 Version History Recovery
│
│ PENDING
▼
E2E-10 Audit History Recovery
│
│ PENDING
▼
E2E-11 Persistent End-to-End Test
│
│ PENDING
▼
IA-06 Persistence Test Conformance
│
│ PENDING
▼
IA-07 E2E-GATE-01
│
│ PENDING
▼
IA-08 Implementation Conformance

## Current Status Summary

| Phase                                  | Status       | Progress |
| -------------------------------------- | ------------ | -------: |
| IA-01 Domain Semantic Alignment        | COMPLETE     |     100% |
| IA-02 Contract and Lifecycle Alignment | COMPLETE     |     100% |
| IA-03 Persistence Model                | COMPLETE     |     100% |
| IA-04 Concrete Repository Layer        | COMPLETE     |     100% |
| IA-05 Database Wiring                  | COMPLETE     |     100% |
| E2E-07 Production Persistence          | COMPLETE     |     100% |
| E2E-08 Persistent Reload               | NEXT         |       0% |
| E2E-09 Version History Recovery        | PENDING      |       0% |
| E2E-10 Audit History Recovery          | PENDING      |       0% |
| E2E-11 Persistent End-to-End Test      | PENDING      |       0% |
| IA-06 Persistent Integration Tests     | IN PROGRESS  |  PARTIAL |
| IA-07 E2E-GATE-01                      | PENDING      |       0% |
| IA-08 Implementation Conformance       | PENDING      |       0% |

## Last Completed Gate

`E2E-07 — PRODUCTION PERSISTENCE — COMPLETE — VERIFIED`

Checkpoint:

`b00d5c7`

Verification:

`70 passed`

## Current Work Package

`E2E-08 — PERSISTENT RELOAD — NEXT`

## Next Action

Implement and verify:

`E2E-08 — Persistent Reload`

The test must:

1. persist a LearningObject through the production persistence path;
2. commit the transaction;
3. close the original session;
4. create a new independent session;
5. reload the LearningObject through the concrete repository;
6. verify reconstructed aggregate state.

Then proceed one concrete E2E block at a time using:

`inspect → block/sections → verify → test → percentage → next gap`

## Operating Rule

No implementation change is considered complete until its focused test passes.

For an existing file with a small, localized change, identify only the sections that need modification instead of rewriting the whole file.

For a new file, provide one complete block for that file.

If a single-file change is too large for one block, split it into sequential blocks while keeping all blocks scoped to that same file.

Distinct files are always handled as distinct blocks.

If a repository read or search fails, automatically try alternative Git tree, path, or lookup methods before stopping.

After a focused test passes, advance automatically to the next confirmed gap and update the completion percentage.

Do not reopen a completed IA phase unless new authoritative evidence establishes a direct contradiction with the normative architecture or roadmap.


IA-01, IA-02, IA-03, IA-04 and IA-05 are closed.

E2E-07 Production Persistence is complete and verified at checkpoint `b00d5c7` with `70 passed`.

The current implementation boundary is:

`Application → Concrete Repository → Persistence Model → Database`

The next unclosed boundary is:

`Database → New Session → Repository → Reconstructed Aggregate`

Therefore the next confirmed gap is:

`E2E-08 — Persistent Reload`
