# 04_End_to_End_Gap_Map.txt

## End-to-End Gap Map

### Purpose

This document defines the gap between the current implementation baseline and the intended persistent end-to-end ELR flow.

It is a continuity artifact and does not replace the authoritative specifications.

---

## IA-01 Closure Mapping

The following IA-01 findings are reflected in the current E2E baseline:

* IA-001 — KnowledgeStatement is an explicit immutable LearningObject aggregate member.
* IA-002 — KnowledgeStatement has no autonomous identity or lifecycle boundary.
* IA-003 — Version is explicitly owned by LearningObject through `learning_object_id`.
* IA-005 — Version state is no longer mutable state stored directly on LearningObject.
* IA-010 — Version history is LearningObject-centered.
* IA-017 — Version snapshots preserve structured KnowledgeStatement semantics.
* IA-018 — Application and repository Version history boundaries are aligned.
* IA-019 — Version snapshot semantics are covered by application tests.

All eight findings are:

`RESOLVED`

---

## Target End-to-End Flow

The target flow is:

Anchor
  ↓
LearningObject
  ↓
KnowledgeStatement aggregate member
  ↓
Candidate
  ↓
Proposed
  ↓
Active
  ↓
Retired
  ↓
Immutable Version
  ↓
Audit Record
  ↓
Persistent Storage
  ↓
Reload
  ↓
Version History
  ↓
Audit History

---

## E2E-01 — Anchor Creation

### Target

Create an Anchor with stable identity and independent persistence.

### Current State

* Anchor domain entity exists.
* Anchor repository exists.
* Anchor persistence model exists.
* Anchor persistence test exists.

### Status

`COMPLETE`

## E2E-02 — LearningObject Creation

### Target

Create a LearningObject with stable identity and a representation conformant with the KnowledgeStatement boundary.

### Current State

* Domain creation exists.
* Application creation exists.
* In-memory persistence exists.
* Production persistence model is absent.

### IA Mapping

* IA-001 — RESOLVED
* IA-002 — RESOLVED

### Status

`APPLICATION COMPLETE / PERSISTENCE GAP`

---

## E2E-03 — KnowledgeStatement Integration

### Target

Represent KnowledgeStatement according to its aggregate-member/value semantics inside LearningObject.

### Current State

KnowledgeStatement is represented as an immutable domain value object owned by LearningObject.

It has no autonomous repository, lifecycle, or versioning boundary.

### IA Mapping

* IA-001 — RESOLVED
* IA-002 — RESOLVED

### Status

`COMPLETE`

---

## E2E-04 — Lifecycle Progression

### Target

Execute:

`Candidate → Proposed → Active → Retired`

while rejecting invalid transitions.

Change Proposal review decisions remain separate from LearningObject lifecycle state.

### Current State

Domain transitions, application orchestration, and tests exist.

The LearningObject does not use `Reviewed`, `Approved`, `Rejected`, or `Updated` as lifecycle states.

### IA Mapping

* IA-005 — RESOLVED
* IA-016 — RESOLVED

### Status

`APPLICATION COMPLETE / PERSISTENCE GAP`

---

## E2E-05 — Immutable Version Creation

### Target

Create an immutable Version representing the appropriate LearningObject state.

### Current State

Version creation exists in the application layer and is tested through the in-memory repository.

The Version snapshot contains the structured KnowledgeStatement representation.

The Version repository is explicitly LearningObject-centered.

Production Version persistence remains absent.

### IA Mapping

* IA-003 — RESOLVED
* IA-010 — RESOLVED
* IA-017 — RESOLVED
* IA-018 — RESOLVED
* IA-019 — RESOLVED

### Status

`APPLICATION COMPLETE / PERSISTENCE GAP`

---

## E2E-06 — Audit Creation

### Target

Create an immutable operational AuditRecord for lifecycle operations.

### Current State

AuditRecord creation and in-memory storage are implemented and tested.

Production AuditRecord persistence remains absent.

### Status

`APPLICATION COMPLETE / PERSISTENCE GAP`

---

## E2E-07 — Production Persistence

### Target

Execute:

Application
    ↓
Domain Repository
    ↓
Concrete Repository
    ↓
Persistence Model
    ↓
Database

### Current State

The verified production persistence tree materializes Anchor only.

The tested application path terminates in in-memory repositories.

### Status

`BLOCKED`

---

## E2E-08 — Persistent Reload

### Target

Reload the LearningObject, its Versions, and its Audit history from persistent storage.

### Current State

No verified persistent round-trip exists.

### Status

`NOT IMPLEMENTED`

---

## E2E-09 — Version History Recovery

### Target

After persistence and reload, retrieve immutable Version history unchanged.

### Current State

History retrieval is tested only against `InMemoryVersionRepository`.

### IA Mapping

* IA-003 — RESOLVED
* IA-010 — RESOLVED
* IA-017 — RESOLVED
* IA-018 — RESOLVED
* IA-019 — RESOLVED

The remaining gap is persistence, not IA-01 domain semantics.

### Status

`NOT IMPLEMENTED`

---

## E2E-10 — Audit History Recovery

### Target

After persistence and reload, recover operational history.

### Current State

Audit history exists only in-memory in the verified implementation baseline.

### Status

`NOT IMPLEMENTED`

---

## E2E-11 — Persistent End-to-End Test

### Target

A single integration test demonstrates:

Create Anchor
    ↓
Create LearningObject
    ↓
Candidate
    ↓
Proposed
    ↓
Active
    ↓
Retired
    ↓
Persist Version
    ↓
Persist Audit
    ↓
Discard runtime state
    ↓
Reload
    ↓
Verify LearningObject
    ↓
Verify Version history
    ↓
Verify Audit history
    ↓
Verify Anchor reference

### Current State

No such persistent integration test exists.

### IA Mapping

IA-001, IA-002, IA-003, IA-005, IA-010, IA-017, IA-018, and IA-019 are already resolved at the domain/application/in-memory level.

### Status

`NOT IMPLEMENTED`

---

# E2E-GATE-01 — Persistent LearningObject Lifecycle

The first decisive end-to-end implementation gate is:

> A LearningObject can be created, progressed through the normative lifecycle, versioned immutably, audited, persisted, reloaded, and have its Version and Audit history recovered without semantic loss.

### IA-01 Preconditions

1. IA-001 — RESOLVED
2. IA-002 — RESOLVED
3. IA-003 — RESOLVED
4. IA-005 — RESOLVED
5. IA-010 — RESOLVED
6. IA-017 — RESOLVED
7. IA-018 — RESOLVED
8. IA-019 — RESOLVED

### Gate Prerequisites

1. KnowledgeStatement representation aligned.       COMPLETE
2. Version boundary aligned.                        COMPLETE
3. LearningObject persistence implemented.          PENDING
4. Version persistence implemented.                 PENDING
5. Audit persistence implemented.                   PENDING
6. Concrete repositories implemented.               PENDING
7. Database wiring implemented.                     PENDING
8. Persistent integration tests implemented.        PENDING

### Gate Status

`PENDING`

---

## End-to-End Conclusion

The current implementation contains a tested in-memory vertical slice but does not yet contain the persistent end-to-end path.

IA-01 is complete and must not be reopened unless contradictory repository evidence is found.

IA-02 — Contract and Lifecycle Alignment is technically resolved and must not be reopened unless contradictory repository evidence establishes a direct contract or lifecycle deviation.

The remaining gaps are persistence, concrete repository infrastructure, database wiring, persistent reload, and persistent end-to-end test coverage.

The next implementation objective is:

**Persistent Infrastructure → Persistent Reload → E2E-GATE-01**
