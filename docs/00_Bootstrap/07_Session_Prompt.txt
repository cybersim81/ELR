# 07_Session_Prompt.txt

## Session Prompt

### Purpose

This document is the operational entry point for a new ELR implementation
session.

A new session MUST read the complete Bootstrap Pack before making any
implementation decision.

The Bootstrap Pack provides:

* normative baseline state;
* implementation baseline inventory;
* implementation finding register;
* E2E gap map;
* implementation alignment roadmap;
* session continuity state;
* this operational session prompt.

The authoritative normative specifications remain the final authority in case
of contradiction.

---

================================================================================

# Current Implementation State

================================================================================

ELR is currently in:

`IA-08 — IMPLEMENTATION CONFORMANCE`

Status:

`IN PROGRESS`

IA-01 through IA-07 are complete or verified according to the current
implementation roadmap and continuity state.

The current implementation work is NOT a new architectural redesign.

The current task is responsibility-level conformance inspection against the
authoritative normative corpus.

---

## Completed IA-08 Finding

`IA-08-A01 — Application Service`

Status:

`COMPLETE / CLOSED`

A01 responsibility disposition:

* A01.2 — Review / Change Proposal
  `IMPLEMENTATION VERIFIED / RESOLVED`;

* A01.3 — Category Service
  `CONFORMANT`;

* A01.4 — Version Service
  `CONFORMANT`;

* A01.5 — Audit Service
  `CONFORMANT`;

* A01.6 — Application Error Boundary
  `RESOLVED`;

* A01.7 — Transaction Boundary
  `IMPLEMENTATION VERIFIED / RESOLVED`.

No remaining non-resolved A01 responsibility is currently identified.

A01 MUST NOT be reopened unless new authoritative or repository evidence
demonstrates a direct contradiction, regression, or previously undiscovered
responsibility.

---

## Verification State

The completed A01 implementation has been verified through the existing
application-service and focused verification tests.

Current complete-suite result:

`123 passed`

A01 verification includes:

* Review / Change Proposal coordination;
* Category Service behavior;
* Version behavior and persistence;
* Audit behavior and persistence;
* Application Error Boundary behavior;
* transactional approval behavior;
* rollback behavior when Audit persistence fails;
* commit behavior when all persistence operations succeed.

---

================================================================================

# Current Finding

================================================================================

`IA-08-A02 — Transactional Event Record`

Status:

`NEXT`

The new session MUST begin by inspecting IA-08-A02.

Do NOT begin by re-inspecting IA-08-A01 unless contradictory evidence is
discovered.

---

## IA-08-A02 Objective

Determine whether the repository implementation conforms to the authoritative
normative responsibility for the Transactional Event Record.

The inspection MUST establish:

1. what the authoritative specifications require;
2. what the current repository actually implements;
3. which responsibilities already exist;
4. which responsibilities are incomplete or absent;
5. whether any apparent difference is architectural or merely naming;
6. whether a confirmed implementation gap actually exists.

Do not introduce a new service, entity, repository, boundary, or abstraction
merely because a particular class or filename is absent.

The absence of a named class is NOT sufficient evidence of non-conformance.

Responsibility and behavior are authoritative; naming is secondary.

---

================================================================================

# Required Working Method

================================================================================

Follow this sequence:

`inspect → compare → responsibility disposition → implement only confirmed gap → test → verify → checkpoint → roadmap/bootstrap update → next finding`

### Step 1 — Normative Inspection

Read the authoritative normative specifications relevant to IA-08-A02.

Identify:

* required responsibility;
* required semantics;
* persistence expectations;
* transaction expectations;
* lifecycle/event expectations;
* actor/context requirements;
* required integration points;
* explicit invariants.

Do not infer requirements from filenames alone.

### Step 2 — Repository Inspection

Inspect the current repository implementation.

Search for:

* domain entities;
* application services;
* repositories;
* persistence models;
* transaction boundaries;
* event or audit representations;
* existing tests;
* adapters;
* wiring;
* existing integration points.

Prefer existing implementation boundaries when they already satisfy the
normative responsibility.

### Step 3 — Responsibility Disposition

Classify the inspected responsibility as one of:

`CONFORMANT`

`IMPLEMENTATION VERIFIED / RESOLVED`

`PARTIAL`

`GAP`

`BLOCKED`

Use the smallest classification supported by evidence.

Do not create implementation work merely to eliminate a naming difference.

### Step 4 — Implementation

If and only if a confirmed implementation gap exists:

* modify only the necessary repository files;
* preserve previously verified behavior;
* preserve A01 closure;
* avoid unrelated refactoring;
* avoid speculative architecture;
* keep the transaction model coherent;
* maintain existing repository abstractions where sufficient.

### Step 5 — Verification

After implementation:

* run focused tests for the changed responsibility;
* run relevant application-service tests;
* run the complete test suite;
* record exact verification results.

A responsibility is not considered complete merely because the code appears
correct.

### Step 6 — Documentation

After verification:

* update the relevant IA-08 implementation roadmap;
* update the current status;
* update the Bootstrap Pack operational state;
* update the session continuity state;
* update the session prompt if the next resume point changes.

Do not modify baseline documents unless the baseline itself has materially
changed.

### Step 7 — Checkpoint

A checkpoint MUST be established after a responsibility receives a final
disposition and verification is complete.

The checkpoint MUST identify:

* completed finding;
* responsibility status;
* verification result;
* remaining findings;
* next finding;
* files changed;
* relevant commit/checkpoint reference when available.

---

================================================================================

# Bootstrap Pack Consistency Rule

================================================================================

The following operational documents MUST remain mutually consistent:

`05_Implementation_Alignment_Roadmap.txt`

`06_Session_Continuity_State.txt`

`07_Session_Prompt.txt`

They MUST describe the same:

* current IA-08 finding;
* completed IA-08 findings;
* current status;
* verification state;
* next action.

Baseline documents such as 01, 02, 03 and 04 MUST NOT be modified merely to
reflect an operational checkpoint unless their actual baseline content has
changed.

---

================================================================================

# Current IA-08 Roadmap

================================================================================

`IA-08-A01 — Application Service`
`COMPLETE / CLOSED`

`IA-08-A02 — Transactional Event Record`
`NEXT`

`IA-08-A03 — Security and Authorization`
`PENDING`

`IA-08-A04 — API Implementation`
`PENDING`

Current sequence:

`A01 CLOSED → A02 NEXT → A03 PENDING → A04 PENDING`

---

================================================================================

# Explicit Do-Not-Reopen Rule

================================================================================

Unless new contradictory evidence is discovered, the next session MUST NOT
restart:

* specification propagation review;
* implementation baseline inventory;
* E2E gap discovery;
* IA-01 Domain Semantic Alignment;
* IA-02 Contract and Lifecycle Alignment;
* IA-03 Persistence Model;
* IA-04 Concrete Repository Integration;
* IA-05 Database Wiring;
* IA-06 Persistence Test Conformance;
* IA-07 E2E-GATE-01;
* IA-08-A01 Application Service.

The following A01 responsibilities are closed:

* A01.2;
* A01.3;
* A01.4;
* A01.5;
* A01.6;
* A01.7.

They MUST remain closed unless new authoritative or repository evidence
establishes a contradiction or regression.

---

================================================================================

# Repository Modification Rule

================================================================================

The user performs repository modifications directly.

The assistant MUST NOT modify the repository directly unless explicitly
authorized.

When implementation changes are required, provide:

* exact file path;
* exact destination;
* complete replacement block when appropriate;
* exact insertion point;
* test command;
* expected verification purpose.

If a file contains many dispersed changes and a complete rewrite is safer,
provide the complete file content instead.

Prefer minimal, controlled changes.

---

================================================================================

# Test and Verification Rule

================================================================================

Every implementation change MUST be verified.

Focused tests MUST pass before considering the responsibility complete.

The complete test suite MUST be executed before declaring a finding resolved or
conformant.

Current baseline verification:

`123 passed`

Do not claim a new verification result until the test has actually been run.

---

================================================================================

# New Session Resume Instruction

================================================================================

A new session MUST:

1. read the complete Bootstrap Pack;
2. read the current IA-08 implementation conformance roadmap;
3. verify repository HEAD and current branch state;
4. confirm that the Bootstrap Pack operational documents agree;
5. resume at:

`IA-08-A02 — Transactional Event Record`

The new session MUST NOT assume that A02 is a gap.

It MUST first inspect and compare the authoritative requirement with the
existing implementation.

---

================================================================================

# Frozen Resume Point

================================================================================

`IA-08 — IMPLEMENTATION CONFORMANCE`

`IA-08-A01 — COMPLETE / CLOSED`

`A01.2 — IMPLEMENTATION VERIFIED / RESOLVED`

`A01.3 — CONFORMANT`

`A01.4 — CONFORMANT`

`A01.5 — CONFORMANT`

`A01.6 — RESOLVED`

`A01.7 — IMPLEMENTATION VERIFIED / RESOLVED`

`A01 VERIFICATION — 123 PASSED`

`IA-08-A02 — TRANSACTIONAL EVENT RECORD — NEXT`

`IA-08-A03 — SECURITY AND AUTHORIZATION — PENDING`

`IA-08-A04 — API IMPLEMENTATION — PENDING`

---

================================================================================

# Final Resume Instruction

================================================================================

**RESUME AT: IA-08-A02 — TRANSACTIONAL EVENT RECORD**

**DO NOT REOPEN IA-08-A01 WITHOUT CONTRADICTORY EVIDENCE.**

**DO NOT ASSUME IA-08-A02 IS A GAP.**

**INSPECT THE AUTHORITATIVE REQUIREMENT FIRST.**

**INSPECT THE EXISTING REPOSITORY IMPLEMENTATION SECOND.**

**DISPOSE THE RESPONSIBILITY BASED ON EVIDENCE.**

**IMPLEMENT ONLY A CONFIRMED GAP.**

**VERIFY FOCUSED TESTS.**

**VERIFY THE COMPLETE TEST SUITE.**

**UPDATE ROADMAP AND BOOTSTRAP STATE AFTER VERIFICATION.**

**ESTABLISH THE NEXT CHECKPOINT BEFORE MOVING TO THE NEXT FINDING.**
