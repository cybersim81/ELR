# English Learning Repository (ELR)

## Continuity Package

**Version:** 2.1.0
**Status:** ACTIVE
**Document Type:** Session Continuity and Architectural Recovery Package

---

# 1. PURPOSE

This document defines the persistent continuity model for ELR architectural
work across separate sessions.

Its purpose is to ensure that a new session can reconstruct the current
working state from repository evidence without relying on conversational
memory.

The repository is the source of truth.

This document does not replace:

* Architecture Specification;
* Architecture Decision Log;
* Architecture Consistency Matrix;
* domain specifications;
* implementation specifications;
* Roadmap;
* Handoff;
* Session Prompt.

It defines how those artifacts are used together for continuity.

---

# 2. SOURCE OF TRUTH

The ELR repository is the authoritative source for:

* document existence;
* document content;
* document version;
* document status;
* canonical document identity;
* current versioned filename;
* architecture decisions;
* implementation state;
* roadmap state;
* continuity state.

Conversational memory must not override repository evidence.

If the current repository state differs from a previous conversation, the
repository state is authoritative.

---

# 3. CANONICAL DOCUMENT IDENTITY

ELR continuity artifacts distinguish between:

* **canonical logical name**;
* **versioned repository filename**.

The canonical logical name identifies the artifact independently of version.

The versioned repository filename identifies the current physical document
version stored in the repository.

A version change does not create a new canonical identity.

---

# 4. CANONICAL NAME → CURRENT FILE RESOLUTION

Current canonical-to-file mapping:

| Canonical name                           | Current repository file                            |
| ---------------------------------------- | -------------------------------------------------- |
| `ELR Architectural Work State — Handoff` | `ELR Architectural Work State - Handoff 6.0.0.txt` |
| `ELR Continuity Package`                 | `ELR Continuity Package 2.1.0.txt`                 |
| `ELR Roadmap`                            | `ELR Roadmap 1.2.1.txt`                            |
| `ELR Session Prompt`                     | `ELR Session Prompt 1.2.0.txt`                     |

Resolution rule:

**canonical name → repository lookup → current versioned filename**

When a document is referenced semantically, use the canonical name.

When a document must be read, modified or verified, resolve the canonical
name to the current versioned filename using repository evidence.

Never assume that a previous versioned filename remains current.

---

# 5. VERSION RESOLUTION RULES

When opening or modifying a continuity artifact:

1. identify the canonical document name;
2. inspect the repository;
3. determine the current versioned filename;
4. verify the version contained in the document;
5. verify that all continuity references point to the current version;
6. only then perform READ, WRITE or VERIFY operations.

A reference to a canonical name must not silently imply a historical version.

A reference to a versioned filename must be verified explicitly against the
repository.

---

# 6. CONTINUITY ARTIFACT RESPONSIBILITIES

The four primary continuity artifacts have different responsibilities.

## 6.1 ELR Session Prompt

Canonical name:

`ELR Session Prompt`

Current file:

`ELR Session Prompt 1.2.0.txt`

Responsibility:

Defines how a new session must bootstrap, verify the repository and work.

---

## 6.2 ELR Continuity Package

Canonical name:

`ELR Continuity Package`

Current file:

`ELR Continuity Package 2.1.0.txt`

Responsibility:

Defines the persistent continuity model and recovery rules.

---

## 6.3 ELR Architectural Work State — Handoff

Canonical name:

`ELR Architectural Work State — Handoff`

Current file:

`ELR Architectural Work State - Handoff 6.0.0.txt`

Responsibility:

Records the latest verified operational checkpoint and the next authorized
action.

---

## 6.4 ELR Roadmap

Canonical name:

`ELR Roadmap`

Current file:

`ELR Roadmap 1.2.1.txt`

Responsibility:

Defines architectural phases, sequencing and future gates.

---

# 7. CURRENT ARCHITECTURAL STATE

The specification consistency consolidation has been completed.

The final verification covered 15 ELR documents.

Verified boundaries include:

* Domain Model ↔ Core Domain Implementation;
* Persistence Model ↔ Persistence Implementation;
* Lifecycle ↔ Domain ↔ Application;
* Event Model ↔ Event Implementation ↔ Persistence;
* Security Model ↔ Security Implementation ↔ API;
* API Specification ↔ API Implementation;
* Architecture ↔ implementation layers;
* Import / Export ↔ Domain / Persistence;
* Learning Object ↔ Anchor ↔ Knowledge Statement;
* Learning Activity ↔ Exercise System ↔ Evidence;
* Knowledge Extraction Pipeline ↔ Change Evidence ↔ Learning Review;
* Evidence ↔ Mastery Engine ↔ Mastery State.

The final consistency corrections were verified.

Final result:

**Finding aperti: 0**

The specification consistency checkpoint is:

**GO**

The consolidated specifications are considered frozen with respect to the
architectural boundaries and decisions verified during this checkpoint.

---

# 8. CURRENT ROADMAP POSITION

Completed phases:

* Fase 0 — Fondazioni Architetturali
* Fase 1 — ELR Knowledge Model
* Fase 2 — Knowledge Extraction Architecture
* Fase 3 — Evidence Architecture
* Fase 4 — Mastery Architecture
* Fase 5 — Human Learning Control
* Fase 6 — Learning Activity and Exercise Systems
* Fase 7 — Document Consistency Consolidation
* Fase 8 — Implementation Readiness Review

Current phase:

**Fase 9 — Derived Learning Systems**

Fase 8 has been completed and its Implementation Readiness Checkpoint has
been passed.

Fase 9 is therefore the next authorized roadmap position.

The completion of Fase 8 does not imply that implementation work is complete.
Implementation may proceed according to the existing implementation plan
where authorized.

---

# 9. CURRENT NEXT ACTION

The next authorized activity is:

**Fase 9 — Derived Learning Systems**

The implementation-readiness work for ELR Core Repository v0.1 has been
completed.

The existing implementation plan remains the authoritative source for the
implementation sequence:

`docs/06_System/ELR Core Repository Implementation Plan 0.1.0.txt`

The completion of the Implementation Readiness Checkpoint does not replace
the implementation plan and does not constitute implementation completion.

The expected next architectural position is:

**Phase 9 — Derived Learning Systems**

---

# 10. SESSION BOOTSTRAP

A new session must read, in this order:

1. `ELR Session Prompt 1.2.0.txt`
2. `ELR Continuity Package 2.1.0.txt`
3. `ELR Architectural Work State - Handoff 6.0.0.txt`
4. `ELR Roadmap 1.2.1.txt`

The session must then:

1. verify the repository;
2. verify the current version of each continuity artifact;
3. resolve canonical names to current filenames;
4. determine the current phase;
5. read only the specialist documents relevant to the current phase;
6. begin the next authorized activity.

The current phase must not be reconstructed from conversational memory.

A completed checkpoint must not be repeated automatically.

Before reopening a completed checkpoint, verify whether:

* a relevant document has changed;
* a newer document version exists;
* a contradiction has appeared;
* a new finding has appeared;
* implementation evidence has materially changed;
* explicit reassessment has been requested.

Without such evidence, use the recorded checkpoint result and continue from
the next authorized position.

---

# 11. SESSION WORKING PROTOCOL

Use the following sequence:

**repository verification
→ focused discrepancy
→ document analysis
→ one focused proposal
→ explicit approval
→ copyable documentation block
→ repository update
→ repository verification
→ next point**

A proposal is not an applied change until the repository has been reread and
the modified content has been verified.

Completed checkpoints should not be re-executed merely to reconstruct
context.

---

# 12. DOCUMENT PROPAGATION RULE

When a document is changed:

1. identify its canonical identity;
2. identify its current versioned filename;
3. identify all dependent continuity artifacts;
4. update only references affected by the version/path change;
5. preserve canonical identity;
6. reread the modified documents;
7. verify canonical name, filename and version consistency.

Propagation must be controlled.

Do not update unrelated documents merely because they mention the same
concept.

---

# 13. ARCHITECTURAL AUTHORITY

The following principles remain mandatory:

* the repository is the source of truth;
* Architecture Specification defines architectural responsibility;
* Architecture Decision Log records architectural decisions;
* Architecture Consistency Matrix records cross-document consistency;
* specifications define detailed behavior;
* implementation does not acquire architectural authority merely because it
  exists;
* persistence structures must not be used to infer Domain semantics;
* Domain semantics must not be inferred from ORM structures;
* APPROVED architectural decisions must not be reopened without a real
  documented contradiction.

---

# 14. APPROVED DECISION RULE

A decision is considered authoritative only when:

* it exists in the appropriate authoritative document;
* its status is verified;
* it is consistent with the Architecture Decision Log;
* no unresolved contradiction exists.

A new architectural decision requires:

**problem → verification → proposal → explicit approval → documentation →
verification**

---

# 15. FROZEN SPECIFICATION RULE

The specifications consolidated during the Specification Consistency
checkpoint are considered frozen with respect to their verified
architectural boundaries.

They must not be reopened merely to:

* improve wording;
* introduce implementation preferences;
* accommodate an ORM structure;
* accommodate an existing code structure;
* change a previously approved architectural decision without contradiction.

A real contradiction may trigger a controlled reopening.

Completion of the Implementation Readiness Review does not by itself reopen
the frozen specification set.

---

# 16. IMPLEMENTATION READINESS RULE

Fase 8 existed to determine whether the frozen specifications were
sufficiently precise for implementation.

The review verified:

* completeness of implementation responsibilities;
* contracts between Domain, Application, Persistence, Event, Security and API;
* traceability between specifications and implementation components;
* remaining technical decisions;
* implementation sequencing;
* initial tests and contract checks.

The Implementation Readiness Checkpoint has been completed.

Result:

**GO**

The review did not silently introduce architecture.

Any future unresolved item must still be classified as either:

* already defined by an authoritative Specification;
* an implementation/technical decision;
* an architectural decision requiring explicit approval.

The Implementation Readiness Checkpoint is therefore a completed gate between
the frozen specification set and implementation work.

---

# 17. SESSION CLOSURE CHECKPOINT

A session may propose closure when:

* the current work item is verified;
* no partially applied correction remains;
* the next authorized action is explicit;
* the working state is reconstructable from repository evidence;
* continuity artifacts are internally consistent;
* canonical names resolve to current versioned filenames;
* no unresolved documentation finding remains.

The assistant must explicitly state when a clean checkpoint has been reached.

The assistant must not close the session automatically.

The user must explicitly approve closure.

A completed phase or checkpoint is a valid session stopping point.

---

## 17A. VISIBILITÀ DEL PROGRESSO DI FASE

Il workflow di continuità comprende una mappa sintetica del progresso della
fase utilizzata durante le sessioni di lavoro attive.

La mappa di progresso registra lo stato operativo della fase corrente usando:

* `[ ]` non iniziato;
* `[~]` in elaborazione;
* `[x]` verificato/completato.

La mappa deve identificare:

* la fase corrente;
* i principali punti di lavoro;
* il lavoro completato;
* il lavoro attualmente in elaborazione;
* il lavoro pianificato;
* il checkpoint o gate previsto.

La mappa di progresso non costituisce autorità architetturale e non sostituisce
le Specification, gli ADR, la Architecture Consistency Matrix, la Roadmap o
altri documenti autorevoli.

Il suo scopo è fornire visibilità operativa e supportare la gestione dei
checkpoint.

In prossimità di un checkpoint, la sessione deve rendere espliciti:

* gli elementi verificati;
* gli elementi ancora aperti;
* le decisioni che si sono rese necessarie;
* le apparenti decisioni risolte mediante documenti autorevoli già esistenti;
* la prossima azione autorizzata;
* l'eventuale necessità di aggiornare gli artefatti di continuità.

Questo meccanismo costituisce inoltre una base leggera per valutare
l'efficienza del workflow alla chiusura della sessione.

Un punto della mappa può essere marcato `[x]` solamente quando il lavoro
sottostante è stato verificato secondo le regole di verifica del repository.

La mappa di progresso, da sola, non costituisce evidenza del completamento di
un checkpoint.

---

# 18. CHECKPOINT VERIFICATION

Before declaring a continuity checkpoint complete, verify:

* canonical names;
* current filenames;
* versions;
* statuses;
* current Roadmap phase;
* next authorized action;
* Handoff references;
* Session Prompt references;
* Continuity Package references;
* Roadmap references.

The final verification result must be reported explicitly.

For a checkpoint already recorded as completed, verification should confirm
whether the recorded state remains current rather than automatically
re-executing the entire checkpoint.

---

# 19. CURRENT CHECKPOINT

Checkpoint:

**Implementation Readiness Checkpoint**

Result:

**GO**

Fase 8:

**COMPLETED**

Verified readiness scope:

* implementation responsibilities;
* Domain ↔ Application ↔ Persistence contracts;
* Event contracts;
* Security contracts;
* API contracts;
* specification → implementation traceability;
* remaining technical decisions;
* implementation sequence;
* initial contract checks.

Open findings:

**0**

Blocking architectural decisions remaining:

**0**

New specifications required:

**0**

New matrices required:

**0**

Current Roadmap:

**ELR Roadmap 1.2.1.txt**

Current Handoff:

**ELR Architectural Work State - Handoff 6.0.0.txt**

Current Session Prompt:

**ELR Session Prompt 1.2.0.txt**

Current Continuity Package:

**ELR Continuity Package 2.1.0.txt**

Current phase:

**Fase 9 — Derived Learning Systems**

Next authorized action:

**Fase 9 — Derived Learning Systems**

---

# 20. RECOVERY PRINCIPLE

A new session must be able to reconstruct the current working position
without access to previous conversational context.

Therefore:

* canonical document identity must remain stable;
* current filenames must be resolvable;
* versions must be explicit;
* the Roadmap must define the phase;
* the Handoff must define the operational checkpoint;
* the Continuity Package must define continuity rules;
* the Session Prompt must define bootstrap behavior.

No single continuity artifact is sufficient by itself.

Together they form the ELR continuity system.

---

# 21. END STATE

At the current checkpoint:

**Architecture consolidation → COMPLETE**

**Specification consistency → GO**

**Finding aperti → 0**

**Fase 8 — Implementation Readiness Review → COMPLETE**

**Implementation Readiness Checkpoint → GO**

**Blocking architectural decisions → 0**

**New specifications required → 0**

**New matrices required → 0**

**Current roadmap position → Fase 9 — Derived Learning Systems**

**Session Prompt → 1.2.0**

**Continuity Package → 2.1.0**

**Handoff → 6.0.0**

**Roadmap → 1.2.1**

**Continuity system → canonical naming and version resolution defined**

---

**END OF ELR CONTINUITY PACKAGE**
