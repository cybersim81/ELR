================================================================================
English Learning Repository (ELR)

IA-08-A01 Application Service
Implementation Conformance Resolution

Version: 0.1.0
Status: WORKING IMPLEMENTATION GUIDANCE
================================================================================


# Document Identity

Document Name:

ELR IA-08-A01 Application Service Resolution

Document Version:

0.1.0

Status:

WORKING IMPLEMENTATION GUIDANCE

Parent Plan:

ELR IA-08 Implementation Conformance Gap Resolution Plan 0.1.0

Finding:

IA-08-A01 — Application Service Conformance

Authority Status:

This document is an implementation coordination artifact.

It does not supersede, amend, or introduce normative domain, lifecycle,
versioning, persistence, API, event, security, or architectural rules.

In case of conflict, the applicable authoritative specification prevails.


================================================================================
# 1. Purpose
================================================================================

This document defines the controlled implementation resolution for:

IA-08-A01 — Application Service Conformance.

The objective is to bring the Application Layer into conformance with the
authoritative ELR Application Service Implementation Specification while
preserving all previously verified domain and persistence behavior.

The implementation shall be minimal.

No unrelated architectural or semantic changes are authorized by this
document.


================================================================================
# 2. Conformance Finding Under Inspection
================================================================================

The current implementation contains a LearningObjectService and already
coordinates several Learning Object operations.

Existing capabilities include:

* create_candidate();
* submit_for_review();
* approve();
* update_knowledge();
* retire();
* get();
* get_history().

The current implementation does not yet provide the complete Application
Service structure required by the Application Service Implementation
Specification.

The suspected missing or incomplete areas requiring implementation
inspection are:

* Change Proposal approval/rejection coordination;
* Category Service boundary;
* Version Service boundary;
* Audit Service boundary;
* explicit application transaction coordination;
* application-level error translation.


================================================================================
# 3. Authoritative Requirements
================================================================================

The Application Layer is responsible for coordinating:

* API requests;
* use cases;
* domain rules;
* persistence;
* audit.

The Application Layer is not responsible for:

* cognitive logic;
* database schema;
* HTTP details.

Application Services must:

* represent explicit use cases;
* preserve Domain Layer authority;
* define transaction boundaries;
* coordinate components without replacing their responsibilities.


================================================================================
# 4. Current Implementation Baseline
================================================================================

The current LearningObjectService already demonstrates the intended basic
dependency direction.

It depends on:

* Domain entities;
* Repository interfaces.

It does not directly depend on:

* FastAPI;
* SQLAlchemy;
* PostgreSQL.

This dependency direction must be preserved.


================================================================================
# 5. Resolution Strategy
================================================================================

The A01 resolution shall be handled according to the verified Application
Service conformance matrix.

The implementation sequence follows the responsibilities defined by IA-08-A01:

A01.1 — Candidate / Learning Object
A01.2 — Review / Change Proposal
A01.3 — Category Service
A01.4 — Version Service
A01.5 — Audit Service
A01.6 — Application Error Boundary
A01.7 — Transaction Boundary

The current implementation shall be reconciled against each responsibility
before source changes are introduced.

The resolution strategy is:

1. preserve responsibilities already satisfied by the current implementation;
2. identify confirmed partial boundaries;
3. identify confirmed gaps;
4. keep unsupported responsibilities blocked;
5. implement only confirmed gaps or verified incomplete boundaries;
6. verify each implementation increment before proceeding.

No implementation shall be introduced solely to satisfy a naming difference
between the specification and the existing implementation.

No Domain, Review, Security, persistence, or API semantics shall be invented
inside the Application Layer.


================================================================================
# 22. Implementation Inspection Matrix
================================================================================

This matrix records the verified implementation state of IA-08-A01.

The matrix reconciles:

* the Bootstrap Pack;
* the authoritative IA-08 Application Service specification;
* the current repository structure;
* the current Application Layer implementation;
* the Domain Layer contracts;
* the Persistence Layer capabilities.

A classification shall be based on confirmed implementation evidence.

A structural naming difference alone is not sufficient to establish a gap.


================================================================================
# 22.1 Classification
================================================================================

CONFORMANT

The required responsibility is already satisfied by the current implementation.

PARTIAL

The required responsibility is substantially present, but an explicit
application boundary, capability, or integration remains incomplete.

GAP

A required responsibility is confirmed missing or incorrectly exposed.

BLOCKED

The responsibility cannot be implemented correctly because a required
authoritative upstream capability is not currently available.


================================================================================
# 22.2 Verified Matrix
================================================================================

| Area | Status | Evidence / Finding |
|------|--------|--------------------|
| Candidate / LearningObject use cases | CONFORMANT | LearningObjectService already coordinates candidate creation, retrieval, lifecycle operations, knowledge updates, version coordination, history, and audit recording. |
| Review / Change Proposal | BLOCKED | LearningObject lifecycle operations exist, but no confirmed ChangeProposal / LearningReview process exists to support approve_change_proposal() and reject_change_proposal(). |
| Category Service | PARTIAL | KnowledgeCategory and CategoryRepository exist. The required Application Service boundary exposing get_categories() and get_category_tree() is not yet confirmed. |
| Version Service | PARTIAL | Version entity, VersionRepository, version creation, sequencing, and history coordination already exist through LearningObjectService. Explicit Version Service boundary is not separately established. |
| Audit Service | PARTIAL | AuditRecord, AuditRepository, recording, and retrieval capabilities exist. Explicit Audit Service boundary is not separately established. |
| Application Error Boundary | GAP | LearningObjectNotFound is local to LearningObjectService and InvalidStateTransition crosses the Application boundary without translation. No centralized Application error taxonomy is confirmed. |
| Transaction Boundary | PARTIAL | Persistence transaction infrastructure exists, including transaction context, commit, rollback, and shared session wiring. Explicit Application ownership of the transaction boundary requires further verification. |


================================================================================
# 22.3 A01.1 — Candidate / Learning Object
================================================================================

Status:

CONFORMANT

The existing LearningObjectService already coordinates the implemented
Learning Object use cases.

No new service shall be introduced solely to satisfy a naming difference.


================================================================================
# 22.4 A01.2 — Review / Change Proposal
================================================================================

Status:

BLOCKED

The required operations are:

* approve_change_proposal();
* reject_change_proposal().

No confirmed Change Proposal / Learning Review model or equivalent process
currently exists.

The Application Layer shall not invent replacement Review semantics.

Existing LearningObject lifecycle behavior shall remain unchanged.


================================================================================
# 22.5 A01.3 — Category Service
================================================================================

Status:

PARTIAL

Existing:

* KnowledgeCategory;
* CategoryRepository;
* persistence support.

Required Application boundary:

* get_categories();
* get_category_tree().

Further implementation requires only the minimum Application coordination
needed to expose the existing Category contract.

No new taxonomy semantics shall be introduced.


================================================================================
# 22.6 A01.4 — Version Service
================================================================================

Status:

PARTIAL

Existing LearningObjectService already coordinates:

* Version creation;
* version sequencing;
* version history;
* VersionRepository access.

The existence of these responsibilities means that a separate VersionService
shall not be created merely because the specification names a service.

Further action requires confirming whether an explicit Version Service
boundary provides a materially required application responsibility.


================================================================================
# 22.7 A01.5 — Audit Service
================================================================================

Status:

PARTIAL

Existing capabilities include:

* AuditRecord;
* AuditRepository;
* audit recording;
* audit retrieval.

The explicit Application Service boundary:

* record_event();
* get_events();

is not separately established.

Further implementation shall preserve the existing AuditRecord and
AuditRepository contracts.


================================================================================
# 22.8 A01.6 — Application Error Boundary
================================================================================

Status:

CONFORMANT

Confirmed finding:

The Application Layer previously had an incomplete error boundary.

Resolution:

The Application Layer now provides a centralized error taxonomy including:

* ApplicationError;
* EntityNotFound;
* InvalidOperation;
* ValidationFailure;
* UnauthorizedOperation.

The service-local LearningObjectNotFound exception has been removed and
replaced by EntityNotFound.

InvalidStateTransition is translated at the Application boundary into
InvalidOperation.

The original Domain exception is preserved through exception chaining.

No catch-all exception translation has been introduced.

No Domain lifecycle semantics have been changed.


Evidence:

* app/application/errors.py;
* app/application/services/learning_object_service.py;
* app/application/services/__init__.py;
* tests/application/services/test_learning_object_service.py.


Verification:

* focused A01.6 tests pass;
* complete pytest suite passes with 79 tests;
* E2E-GATE-01 is VERIFIED / CLOSED according to
  docs/00_Bootstrap/04_End_to_End_Gap_Map.txt.


Resolution:

A01.6 is RESOLVED.


================================================================================
# 22.9 A01.7 — Transaction Boundary
================================================================================

Status:

PARTIAL

Existing Persistence infrastructure includes:

* transaction context;
* commit;
* rollback;
* session ownership;
* repository session wiring.

The remaining conformance question is whether Application-level write
use cases explicitly own transaction coordination as required by IA-08-A01.

No replacement transaction infrastructure shall be introduced until this
boundary is verified.


================================================================================
# 22.10 Implementation Priority
================================================================================

A01.6 — Application Error Boundary

Status:

RESOLVED


The following findings remain PARTIAL and require subsequent focused
verification:

A01.3 — Category Service
A01.4 — Version Service
A01.5 — Audit Service
A01.7 — Transaction Boundary


The following finding remains BLOCKED:

A01.2 — Review / Change Proposal


No further implementation work is required for A01.6 unless new evidence
demonstrates a regression or a conflict with the authoritative specification.


================================================================================
# 22.11 Explicit Non-Goals
================================================================================

This inspection does not authorize implementation of:

* ChangeProposal;
* LearningReview;
* Security policy;
* authorization mechanisms;
* replacement transaction infrastructure;
* new Category domain semantics;
* new Version domain semantics;
* API error handlers.


================================================================================
# 22.12 Verification Rule
================================================================================

A finding may move from GAP or PARTIAL to CONFORMANT only after:

1. implementation evidence is confirmed;
2. focused tests pass;
3. regression tests pass;
4. the relevant architectural boundary remains intact.


================================================================================
# 22.13 Current Status
================================================================================

Finding:

IA-08-A01 — Application Service Conformance

Status:

IN PROGRESS


RESOLVED:

A01.6 — Application Error Boundary


PARTIAL:

A01.3 — Category Service
A01.4 — Version Service
A01.5 — Audit Service
A01.7 — Transaction Boundary


BLOCKED:

A01.2 — Review / Change Proposal



================================================================================
# 22.14 Next Controlled Action
================================================================================

A01.6 — Application Error Boundary is RESOLVED.

The next implementation action shall address the next confirmed
non-resolved A01 responsibility according to the verified conformance
matrix.

Current candidates are:

A01.3 — Category Service
A01.4 — Version Service
A01.5 — Audit Service
A01.7 — Transaction Boundary


The next action shall be selected only after focused inspection of the
relevant responsibility and confirmation that the required capability is
actually missing or incomplete.

No A01.3, A01.4, A01.5, or A01.7 implementation shall be mixed into the
resolved A01.6 change.


================================================================================
End of Document
================================================================================
