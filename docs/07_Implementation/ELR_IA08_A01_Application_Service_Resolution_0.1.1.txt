# IA-08-A01 — Application Service Resolution

**Version:** 0.1.1
**Status:** ACTIVE — PARTIAL CONFORMANCE

## Current Disposition

IA-08-A01 is **not fully closed**.

The application-service layer contains implemented responsibilities for:

* Learning Object creation;
* Learning Object submission for review;
* Learning Object approval;
* immutable Version creation;
* Learning Object knowledge update;
* AuditRecord creation;
* EventRecord creation for Learning Object updates;
* ChangeProposal creation;
* Learning Review invocation;
* transactional persistence boundaries where explicitly verified.

The remaining A01 status shall therefore be assessed **by responsibility**, rather than by treating the entire A01 area as either open or closed.

## Responsibility Status

### A01.1 — Learning Object Creation

**CONFORMANT**

Evidence includes:

* `LearningObjectService.create_candidate()`;
* Learning Object repository persistence;
* creation audit record;
* corresponding application-service tests.

### A01.2 — Change Proposal / Learning Review Boundary

**IMPLEMENTED / CONFORMANCE VERIFICATION PENDING**

The current application-service implementation establishes the required Change Proposal / Learning Review boundary.

The previous implementation-level `BLOCKED` condition has been resolved.

The repository contains the application components required to establish the review-gated change flow:

* `ChangeProposal`;
* `ChangeType`, including `CREATE`, `UPDATE`, and `MERGE`;
* `ChangeProposalService`;
* `LearningReviewService`;
* `LearningReviewAdapter`;
* `LearningObjectChangeApplier`;
* ChangeProposal persistence;
* Review Decision Trace persistence;
* review validation and decision services;
* focused application and review tests.

`ChangeProposalService` coordinates the use case in the following order:

```text
Change Proposal
        │
        v
Learning Review
        │
        ├── APPROVE ───────────→ LearningObjectChangeApplier.apply()
        │
        ├── REJECT ─────────────→ no apply()
        │
        └── REQUEST_REVISION ───→ no apply()
```

The application service therefore does not apply a Change Proposal before Learning Review has produced an `APPROVE` decision.

`REJECT` and `REQUEST_REVISION` do not invoke `LearningObjectChangeApplier.apply()`.

The current implementation and focused tests resolve the previous implementation-level `BLOCKED` condition for the Change Proposal / Learning Review application boundary.

The remaining A01.2 activity is responsibility-level conformance verification of the implemented `CREATE`, `UPDATE`, and `MERGE` semantics against the authoritative `03_Processes` and `06_System` specifications.

Therefore A01.2 remains open only as a **conformance verification item**. It is no longer blocked by absence of the required application boundary or by absence of the `APPROVE` / `REJECT` / `REQUEST_REVISION` gating behavior.

### A01.3 — Learning Object Approval

**CONFORMANT**

Approval transitions a proposed Learning Object to Active and creates the initial immutable Version.

Evidence includes:

* `LearningObject.approve()`;
* `LearningObjectService.approve()`;
* Version creation;
* AuditRecord creation;
* transactional persistence tests;
* rollback verification.

### A01.4 — Version Responsibility

**CONFORMANT**

Version creation is implemented through `VersionService`.

The service determines the next Version number from the repository and persists a new immutable Version.

The existing Version history is preserved.

### A01.5 — Learning Object Update

**CONFORMANT**

`LearningObjectService.update_knowledge()`:

* validates the domain state transition;
* updates the Learning Object;
* creates a new immutable Version;
* records the corresponding AuditRecord;
* records a `LearningObjectUpdated` EventRecord.

Persistence and rollback behavior are covered by focused tests.

### A01.6 — Application Error Boundary

**RESOLVED / CONFORMANT**

The application service translates domain state-transition failures into application-level `InvalidOperation` errors.

The detailed responsibility remains documented by:

`ELR_IA08_A01_06_Application_Error_Boundary_0.2.0.txt`

### A01.7 — Transaction Boundary

**RESOLVED / CONFORMANT**

The verified transactional boundary uses the existing SQLAlchemy Session.

Application services do not autonomously commit the relevant operations.

Rollback tests verify that Learning Object, Version, AuditRecord and EventRecord persistence participate in the expected transaction boundary.

## Final A01 Position

A01 remains **ACTIVE / PARTIAL CONFORMANCE**.

The previous implementation-level `BLOCKED` condition associated with the Change Proposal / Learning Review application boundary is **RESOLVED**.

The implemented decision gate has been verified at implementation level:

```text
APPROVE
    ↓
LearningObjectChangeApplier.apply()

REJECT
    ↓
no apply()

REQUEST_REVISION
    ↓
no apply()
```

The remaining A01.2 activity is **responsibility-level conformance verification** of the implemented `CREATE`, `UPDATE`, and `MERGE` semantics against the authoritative `03_Processes` and `06_System` specifications.


## Status

**IA-08-A01 — ACTIVE / PARTIAL CONFORMANCE**

**Implementation-level BLOCKED condition:** RESOLVED

**Implementation status of Change Proposal / Learning Review boundary:** IMPLEMENTED AND VERIFIED

**Primary remaining verification area:** A01.2 — Change Proposal / Learning Review / CREATE / UPDATE / MERGE semantics

A01 remains open solely pending responsibility-level conformance verification against the authoritative `03_Processes` and `06_System` specifications.

No implementation blocker remains for the Change Proposal / Learning Review application boundary.


================================================================================
Current A01 Verification State
==============================

Questo blocco aggiorna lo stato documentale di A01 sulla base dell'evidenza
repository e test disponibile al checkpoint corrente.

Le sezioni precedenti che descrivono A01.2 come "CONFORMANCE VERIFICATION
PENDING", "EXECUTION PATH MISSING" o equivalente rappresentano la fotografia
precedente al completamento dell'implementazione e non costituiscono più lo
stato corrente.

## A01.2 — Current Verified Position

**STATUS: IMPLEMENTED / VERIFIED**

Il boundary applicativo Change Proposal → Learning Review → ChangeApplier è
presente e verificato.

Il percorso corrente è:

```text
ChangeProposal
        |
        v
Learning Review
        |
        v
ReviewDecisionTrace
        |
        +---- REJECT -------------> no mutation
        |
        +---- REQUEST_REVISION ---> no mutation
        |
        +---- APPROVE
                  |
                  v
          ChangeProposalService
                  |
                  v
       LearningObjectChangeApplier
                  |
          +-------+-------+
          |       |       |
          v       v       v
        CREATE  MERGE   UPDATE
          |       |       |
          +-------+-------+
                  |
                  v
          Version / Audit
````

`LearningObjectChangeApplier` verifica inoltre:

```text
review_trace.proposal_id == proposal.id
review_trace.decision == APPROVE
```

prima dell'applicazione della modifica.

## CREATE

**STATUS: VERIFIED**

Una Change Proposal `CREATE` approvata:

* crea un nuovo Learning Object;
* lo persiste;
* crea la Version iniziale;
* registra l'Audit richiesto.

Evidenza focalizzata:

`tests/application/services/test_learning_object_change_applier.py`

## MERGE

**STATUS: VERIFIED**

Una Change Proposal `MERGE` approvata:

* risolve il Learning Object target;
* modifica il target esistente;
* preserva la sua identità;
* crea la nuova Version;
* registra l'Audit richiesto.

Il percorso è implementato attraverso `LearningObjectChangeApplier`.

## UPDATE

**STATUS: VERIFIED**

Una Change Proposal `UPDATE` approvata:

* risolve il Learning Object target;
* modifica il target esistente;
* preserva la sua identità;
* crea la nuova Version;
* registra l'Audit richiesto.

Il percorso è implementato attraverso `LearningObjectChangeApplier`.

## Negative Paths

Sono verificati i casi in cui:

* la proposta non è APPROVE;
* il Review Decision Trace appartiene a una proposal diversa;
* il target Learning Object non esiste;
* il target identifier è invalido;
* il Knowledge Statement è invalido.

Le proposte non approvate non vengono applicate.

## Verification Evidence

Focused Application Service tests:

`8 passed`

Application test suite:

`48 passed`

Complete repository test suite:

`148 passed`

Risultato:

```text
pytest -q
148 passed
```

## Current A01 Disposition

Alla luce dell'evidenza corrente:

```text
A01.1  CONFORMANT
A01.2  IMPLEMENTED / VERIFIED
A01.3  CONFORMANT
A01.4  CONFORMANT
A01.5  CONFORMANT
A01.6  RESOLVED / CONFORMANT
A01.7  RESOLVED / CONFORMANT
```

La precedente condizione implementation-level `BLOCKED` relativa al boundary
Change Proposal / Learning Review è risolta.

La precedente condizione A01.2 relativa all'assenza dei percorsi applicativi
CREATE / MERGE / UPDATE è risolta.

## Closure Position

L'evidenza repository e test disponibile al checkpoint corrente è sufficiente
a considerare risolto il gap implementativo A01 verificato.

```text
IMPLEMENTATION       VERIFIED
CREATE               VERIFIED
MERGE                VERIFIED
UPDATE               VERIFIED
REVIEW GATE          VERIFIED
VERSIONING           VERIFIED
AUDIT                VERIFIED
APPLICATION TESTS    48 PASSED
FULL TEST SUITE      148 PASSED
```

**Current A01 Status: COMPLETE / CLOSED**

La chiusura si riferisce alla conformità implementativa verificata nel
perimetro A01.

Le successive aree IA-08-A03 e IA-08-A04 rimangono indipendenti e non vengono
chiuse o inferite dalla presente decisione.

================================================================================
End Current A01 Verification State
==================================

